<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gay Chat App - Profile</title>
  <link rel="stylesheet" href="css/styles.css">
</head>
<body>
  <div class="container">
    <h1>{{user.username}}'s Profile</h1>
    <img src="/uploads/{{user.profilePicture}}" alt="Profile Picture">
    <form action="/profile/upload" method="POST" enctype="multipart/form-data">
      <input type="file" name="image">
      <button type="submit">Upload Profile Picture</button>
    </form>

    <h2>Create a Post</h2>
    <form action="/post/create" method="POST" enctype="multipart/form-data">
      <input type="file" name="media">
      <textarea name="text" placeholder="Write something..."></textarea>
      <button type="submit">Post</button>
    </form>

    <h2>Your Posts</h2>
    {{#posts}}
    <div class="post">
      <img src="/uploads/{{media}}" alt="Media">
      <p>{{text}}</p>
      <p>Likes: {{likes}} <button onclick="likePost('{{_id}}')">Like</button></p>
      <form action="/post/comment/{{_id}}" method="POST">
        <input type="text" name="text" placeholder="Write a comment...">
        <button type="submit">Comment</button>
      </form>
      <div class="comments">
        {{#comments}}
        <p>{{text}}</p>
        {{/comments}}
      </div>
    </div>
    {{/posts}}
  </div>
  <script src="js/main.js"></script>
</body>
</html>
